#!/bin/bash
echo "Building ctags... [PWD=$PWD]"
ctags \
  -R \
  --exclude=.mypy_cache \
  --exclude=build \
  --exclude=assets \
  --exclude=target \
  --exclude=docs \
  --exclude=examples \
  .
echo "Tags are complete."
