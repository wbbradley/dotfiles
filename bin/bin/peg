#!/bin/bash
PEGFILE="$HOME/pegged.txt"
if [ $# -eq 0 ]; then
	cat "$PEGFILE"
else
	for f in $@; do
		fullpath=$(realpath "$f")
		if ! grep "$fullpath" "$PEGFILE"; then
			echo $(realpath "$f") >> "$PEGFILE"
		fi
	done
fi
